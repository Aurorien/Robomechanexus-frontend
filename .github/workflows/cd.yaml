# .github/workflows/ci.yml

on:
  push:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Deploy
        run: |
          curl "https://robomechanexus-frontend-test.onrender.com/"
